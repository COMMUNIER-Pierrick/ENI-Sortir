{% extends 'base.html.twig' %}

{% block title %}Inscription via un fichier{% endblock %}

{% block body %}
    <h1>Inscription via un fichier</h1>

    {{ form_start(UploadFileUserForm) }}
    {{ form_widget(UploadFileUserForm) }}
    <button type="submit" class="btn">Valider</button>
    {{ form_end(UploadFileUserForm) }}
{% endblock %}